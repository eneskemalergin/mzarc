# Zig build artifacts
zig-out/
zig-cache/
.zig-cache/

# Local Zig installation (download separately)
zig-*/

# Local zig wrapper (uses project-local zig installation)
/zig

# Editor
.vscode/
*.swp
*~

# Python
.venv/
__pycache__/
*.pyc
*.pyo

# Locally built peer tools
/tools/bin/

# Ignored folders (dev only)
tmp/
temp/
data/
plan/
experiments/

# Benchmark raw timing files (regenerated by benchmark.sh, not needed in git)
benchmark/raw/
# Optional deep zebrac dumps from tools/profile.sh
benchmark/profiling/
